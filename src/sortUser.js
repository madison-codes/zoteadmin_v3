import userData from './mock/.mockusers';

const sortUser = (data) => {
  // data.users.forEach(function(){
    console.log(data.users);
    // return 'test';

// });
  // data.users.sort(function(a, b) {
  //   return a - b;
  // }
// );
};

export default sortUser;
